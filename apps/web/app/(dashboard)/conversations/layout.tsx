import { ConversationLayout } from "@/modules/dashboard/ui/layouts/ConversationLayout";

const Layout = ({ children }: { children: React.ReactNode }) => {
  return <ConversationLayout children={children} />;
};

export default Layout;
    